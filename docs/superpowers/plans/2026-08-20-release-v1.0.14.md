# Release v1.0.14 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Publish the live Codex Apps Jira inventory fix as immutable release `v1.0.14`.

**Architecture:** Follow the existing `v1.0.13` release pattern: update the six version-contract files, validate the complete repository suite, and submit a dedicated release PR. After human review and explicit authorization for that exact PR commit, publish an annotated tag and a `bootstrap.sh` GitHub Release asset generated from the reviewed merge commit.

**Tech Stack:** POSIX shell, Git, GitHub CLI, existing repository tests.

**Spec:** Follow-up to GitHub issue `beroka-vn/beroka-ai-governance#74` and merged PR `beroka-vn/beroka-ai-governance#78`.

## Global Constraints

- Release version is exactly `v1.0.14`; existing tags remain immutable.
- Release source includes merged PR #78 at commit `35855331426984a79c4ac9727f295142c91b481b`.
- GitHub Actions are unverified because account billing prevented jobs from starting; full local tests and live Codex inventory verification are required instead.
- Do not merge the release PR without explicit human authorization for its exact reviewed commit.
- Do not create or push the tag or GitHub Release before the release PR is merged and the exact merge commit passes the release gate.
- Run a fresh `github-write` governance preflight immediately before every GitHub write.

---

### Task 1: Prepare the v1.0.14 Release Contract

**Files:**
- Modify: `VERSION`
- Modify: `README.md`
- Modify: `handbook.md`
- Modify: `PACKAGE-DESIGN.md`
- Modify: `tests/release.sh`
- Modify: `tests/routing.sh`
- Create: `docs/superpowers/plans/2026-08-20-release-v1.0.14.md`

**Interfaces:**
- Consumes: merged `origin/main` commit `35855331426984a79c4ac9727f295142c91b481b`.
- Produces: a reviewed source commit whose version identity is consistently `v1.0.14`.

- [ ] **Step 1: Change release assertions to v1.0.14 and verify RED**

Run `sh tests/release.sh` while `VERSION` remains `v1.0.13`.

Expected: FAIL with `VERSION is not v1.0.14`.

- [ ] **Step 2: Update the release identity**

Set `VERSION`, `README.md`, `handbook.md`, and `PACKAGE-DESIGN.md` to `v1.0.14`.

- [ ] **Step 3: Verify GREEN and the full release gate**

Run:

```bash
for test_file in tests/*.sh; do sh "$test_file"; done
git diff --check
```

Expected: every command exits 0.

- [ ] **Step 4: Commit, push, and open a draft release PR**

Use branch `chore/release-v1.0.14`, commit title `chore: release v1.0.14`, and PR title `chore(release): prepare v1.0.14`. Link PR #78 as release provenance.

### Task 2: Publish the Reviewed Release

**Files:**
- Generate outside the repository: `bootstrap.sh`

**Interfaces:**
- Consumes: the exact reviewed and merged release PR commit.
- Produces: annotated tag `v1.0.14` and GitHub Release `v1.0.14` with one `bootstrap.sh` asset.

- [ ] **Step 1: Stop for exact merge authorization**

Report the release PR number, full head SHA, local verification evidence, and requested squash merge. Continue only after explicit authorization for that exact PR and commit.

- [ ] **Step 2: Verify the merged release commit**

After merge, fetch `origin/main`; confirm `VERSION` is `v1.0.14`, the full local suite passes, and neither the remote tag nor GitHub Release exists.

- [ ] **Step 3: Generate and verify the release asset**

Replace `@RELEASE_VERSION@` and `@RELEASE_COMMIT@` in `release/bootstrap.sh.in` with `v1.0.14` and the exact merge commit. Verify no placeholders remain and `sh -n bootstrap.sh` exits 0.

- [ ] **Step 4: Publish and read back**

Create and push the annotated `v1.0.14` tag, create the GitHub Release with the verified `bootstrap.sh` asset, then read back the tag type, peeled commit, release target, asset name, and asset content markers.
